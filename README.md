# P9

## Tensorboard
Make sure tensorboard is installed, either globally or in virtual environment.
```bash
tensorboard --logdir=path/to/run/logs
```

## Code of conduct
before pull request
git rebase main
